//Exemplo 2 - Autenticação de usuário

let usuarioAutenticado = true

// Autenticando

switch (usuarioAutenticado) {
    case true:
        console.log("Usuário autenticado!"); break;
    case false:
        console.log("Usuário não autenticado!"); break;
}